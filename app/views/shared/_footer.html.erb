<footer>

  <% if user_signed_in? %>
    Signed in as <%= current_user.username %>. <%= link_to "Not you?", logout_path %>
  <% else %>
    <%= link_to_unless_current "Sign up", signup_path %>
    <span class="link-separator">|</span>
    <%= link_to "Log in", login_path %>
  <% end %>

</footer>
