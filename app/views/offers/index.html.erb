<% provide(:title, 'Offers') %>

<h1>Listing offers</h1>

<%= link_to 'New Offer', new_offer_path, class: 'btn btn-blue btn-medium action-btn' %>

<div id="offers_list">
  <% @offers.each do |offer| %>
    <%= render 'offer_summary', offer: offer %>
  <% end %>
</div>

<%= paginate @offers %>

